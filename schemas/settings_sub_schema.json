{
   "$id": "https://silentarctic.github.io/schemas/settings_sub_schema.json",
   "type": "array",
   "description": "Setting(s) in which this item should be used.",
   "items": {
      "type": "object",
      "properties": {
         "name": {
            "type": "string",
            "description": "Setting name."
         },
         "source": {
            "type": "string",
            "description": "Setting source, referencing the source's {@code _meta.source.abbreviation} field. Leave blank to reference the current collection."
         }
      },
      "required": [
         "name"
      ],
      "optional": [
         "source"
      ],
      "additionalProperties": false
   }
}